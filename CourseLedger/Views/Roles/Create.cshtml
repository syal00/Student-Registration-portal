@model CourseLedger.DataAccess.Role
@{ ViewData["Title"] = "Create Role"; }

<h1>Create Role</h1>
<p class="text-muted">Role IDs are assigned manually (not auto-generated).</p>
<div class="row mt-4">
    <div class="col-md-6 col-lg-4">
        <div class="card shadow-sm border-0">
            <div class="card-body p-4">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="mb-3">
                        <label asp-for="Id" class="form-label"></label>
                        <input asp-for="Id" class="form-control mono-id" type="number" min="1" />
                        <span asp-validation-for="Id" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Role1" class="form-label">Role Name</label>
                        <input asp-for="Role1" class="form-control" />
                        <span asp-validation-for="Role1" class="text-danger"></span>
                    </div>
                    <button type="submit" class="btn btn-primary">Create</button>
                    <a asp-action="Index" class="btn btn-outline-secondary ms-2">Cancel</a>
                </form>
            </div>
        </div>
    </div>
</div>
@section Scripts { @{await Html.RenderPartialAsync("_ValidationScriptsPartial");} }
